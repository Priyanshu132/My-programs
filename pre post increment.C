#include<stdio.h>
#include<conio.h>
void main();
{
 int a,sum;
 clrscr();
 while(a!=0)
	{
	   b=a%10;
	   a=a/10;
	   sum=sum+pow(b,3)
	}
	 if(sum==a)
	  printf("number is amstorm");
	getch();
}